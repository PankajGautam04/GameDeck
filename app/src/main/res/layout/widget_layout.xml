<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- Blurred wallpaper background -->
    <ImageView
        android:id="@+id/widget_blur_bg"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="fitXY"
        android:visibility="gone"
        android:contentDescription="@null" />

    <!-- Glass container -->
    <FrameLayout
        android:id="@+id/widget_glass_container"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@drawable/widget_background">

        <!-- Main Game Cover -->
        <ImageView
            android:id="@+id/widget_game_cover"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:scaleType="centerCrop"
            android:contentDescription="@string/game_cover" />

        <!-- GIF Overlay -->
        <ImageView
            android:id="@+id/widget_gif_overlay"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:scaleType="centerCrop"
            android:visibility="gone"
            android:contentDescription="@null" />

        <!-- Invisible Click Targets — 3 Zone Navigation (Prev, Launch, Next) -->
        <LinearLayout
            android:id="@+id/widget_click_targets"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="horizontal">

            <!-- Previous Game Zone (Left 25%) -->
            <FrameLayout
                android:id="@+id/widget_prev_target"
                android:layout_width="0dp"
                android:layout_height="match_parent"
                android:layout_weight="0.25" />

            <!-- Launch Game Zone (Middle 50%) -->
            <FrameLayout
                android:id="@+id/widget_launch_target"
                android:layout_width="0dp"
                android:layout_height="match_parent"
                android:layout_weight="0.5" />

            <!-- Next Game Zone (Right 25%) -->
            <FrameLayout
                android:id="@+id/widget_next_target"
                android:layout_width="0dp"
                android:layout_height="match_parent"
                android:layout_weight="0.25" />
        </LinearLayout>

        <!-- Dynamic Stats (Top-Left Corner) -->
        <LinearLayout
            android:id="@+id/widget_stats_container"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="top|start"
            android:layout_marginTop="12dp"
            android:layout_marginStart="12dp"
            android:layout_marginEnd="12dp"
            android:orientation="vertical"
            android:padding="6dp"
            android:background="#30000000"
            android:visibility="gone">

            <TextView
                android:id="@+id/widget_stat_goal"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textColor="#FFFFFF"
                android:textSize="11sp"
                android:textStyle="bold"
                android:shadowColor="#FF000000"
                android:shadowDx="1"
                android:shadowDy="1"
                android:shadowRadius="3"
                android:visibility="gone" />

            <TextView
                android:id="@+id/widget_stat_heat"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="1dp"
                android:textColor="#FFD700"
                android:textSize="11sp"
                android:textStyle="bold"
                android:shadowColor="#FF000000"
                android:shadowDx="1"
                android:shadowDy="1"
                android:shadowRadius="3"
                android:visibility="gone" />

            <TextView
                android:id="@+id/widget_stat_session"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="1dp"
                android:textColor="#F5F5F5"
                android:textSize="11sp"
                android:textStyle="bold"
                android:shadowColor="#FF000000"
                android:shadowDx="1"
                android:shadowDy="1"
                android:shadowRadius="3"
                android:visibility="gone" />

            <TextView
                android:id="@+id/widget_stat_update"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="1dp"
                android:textColor="#00FFCC"
                android:textSize="11sp"
                android:textStyle="bold"
                android:shadowColor="#FF000000"
                android:shadowDx="1"
                android:shadowDy="1"
                android:shadowRadius="3"
                android:visibility="gone" />
        </LinearLayout>

        <!-- Bottom area: Title + Thumbnail Nav — sits ON TOP of click targets -->
        <LinearLayout
            android:id="@+id/widget_bottom_area"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="bottom"
            android:orientation="vertical"
            android:gravity="bottom">

            <!-- Title & Info row -->
            <LinearLayout
                android:id="@+id/widget_info_container"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:paddingStart="14dp"
                android:paddingEnd="14dp"
                android:paddingBottom="16dp"
                android:orientation="horizontal"
                android:gravity="center_vertical">

                <ImageView
                    android:id="@+id/widget_app_icon"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginEnd="8dp"
                    android:scaleType="centerCrop"
                    android:visibility="gone"
                    android:contentDescription="@null" />

                <LinearLayout
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:orientation="vertical">

                    <TextView
                        android:id="@+id/widget_game_title"
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:textColor="#FFFFFF"
                        android:textSize="14sp"
                        android:textStyle="bold"
                        android:shadowColor="#80000000"
                        android:shadowDx="0"
                        android:shadowDy="2"
                        android:shadowRadius="4"
                        android:maxLines="1"
                        android:ellipsize="end" />

                    <TextView
                        android:id="@+id/widget_playtime"
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:layout_marginTop="1dp"
                        android:textColor="#E0E0E0"
                        android:textSize="10sp"
                        android:shadowColor="#80000000"
                        android:shadowDx="0"
                        android:shadowDy="1"
                        android:shadowRadius="3"
                        android:maxLines="1"
                        android:ellipsize="end"
                        android:visibility="gone" />
                </LinearLayout>
            </LinearLayout>

            <!-- Game Thumbnail Navigation Strip moved to right center -->
        </LinearLayout>

        <!-- Vertical Navigation Dots (Right Center) -->
        <LinearLayout
            android:id="@+id/widget_dots_container"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center_vertical|end"
            android:orientation="vertical"
            android:gravity="center"
            android:paddingEnd="10dp" />

        <TextView
            android:id="@+id/widget_empty_view"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:gravity="center"
            android:text="@string/widget_empty_message"
            android:textColor="#99FFFFFF"
            android:textSize="13sp"
            android:visibility="gone"
            android:background="#80000000"/>

    </FrameLayout>
</FrameLayout>

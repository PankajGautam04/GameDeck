<?xml version="1.0" encoding="utf-8"?>
<!-- Premium game card for config: large cover, overlay controls -->
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="12dp"
    app:cardCornerRadius="20dp"
    app:cardElevation="0dp"
    app:strokeWidth="1dp"
    app:strokeColor="?attr/colorSurfaceVariant">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:padding="14dp">

        <!-- Large cover preview (book ratio) -->
        <ImageView
            android:id="@+id/img_game_cover"
            android:layout_width="72dp"
            android:layout_height="96dp"
            android:contentDescription="@string/game_cover"
            android:scaleType="centerCrop"
            android:background="@drawable/card_background"
            app:shapeAppearanceOverlay="@style/CoverImageLarge" />

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical"
            android:layout_marginStart="16dp">

            <!-- Game label -->
            <TextView
                android:id="@+id/tv_game_label"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:ellipsize="end"
                android:maxLines="2"
                android:textAppearance="?attr/textAppearanceTitleSmall"
                android:letterSpacing="0.01" />

            <!-- GIF badge (hidden by default) -->
            <TextView
                android:id="@+id/tv_gif_badge"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="4dp"
                android:text="@string/gif_badge"
                android:textSize="11sp"
                android:textColor="@color/primary"
                android:textStyle="bold"
                android:visibility="gone" />

        </LinearLayout>

        <!-- Pick image -->
        <ImageButton
            android:id="@+id/btn_pick_cover"
            android:layout_width="44dp"
            android:layout_height="44dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/pick_image"
            android:src="@drawable/ic_image"
            android:padding="8dp" />

        <!-- Pick GIF -->
        <ImageButton
            android:id="@+id/btn_pick_gif"
            android:layout_width="44dp"
            android:layout_height="44dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="Pick GIF"
            android:src="@drawable/ic_dot_active" 
            android:padding="8dp" />

        <!-- Remove -->
        <ImageButton
            android:id="@+id/btn_remove_game"
            android:layout_width="44dp"
            android:layout_height="44dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/remove_game"
            android:src="@drawable/ic_close"
            android:padding="8dp" />

    </LinearLayout>

</com.google.android.material.card.MaterialCardView>
